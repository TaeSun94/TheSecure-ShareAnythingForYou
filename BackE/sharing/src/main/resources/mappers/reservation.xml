<?xml version="1.0" encoding="UTF-8"?>
<!-- mappers/user.xml -->
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.sharing.dao.ReserveDao"> <!-- mybatis는 namespace필수!! -->
	<insert id="addReserve" parameterType="hashmap">
		INSERT INTO reservation (tid, host_num, member_email, reserve_day)
		VALUES (#{tid},#{host_num},#{member_email},#{reserve_day})
	</insert>
</mapper>